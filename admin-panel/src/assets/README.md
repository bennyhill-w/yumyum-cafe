Place the logo image here and name it `logo.jpg`.

- Path: `admin-panel/src/assets/logo.jpg`
- Purpose: used as the site's favicon via the `<link rel="icon">` tag in `admin-panel/index.html`

Replace this README with the actual image file (binary JPEG).
