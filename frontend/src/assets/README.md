Place the logo image here and name it `logo.jpg`.

- Path: `frontend/src/assets/logo.jpg`
- Purpose: used as the site's favicon via the `<link rel="icon">` tag in `frontend/index.html`

Replace this README with the actual image file (binary JPEG).
